import { describe, expect, it } from "vitest";
import { WritingFeedbackSchema } from "@english4free/content-schemas";
import { createWritingDiagnostics } from "./writing-service";
describe("writing diagnostics", () => { it("reports word count and minimum-length status without assigning a band", () => { const diagnostics = createWritingDiagnostics({ promptId: "task", taskType: "IELTS_TASK_2", text: "One two three.", language: "en", expectedMinimumWords: 250 }); expect(diagnostics.wordCount).toBe(3); expect(diagnostics.meetsExpectedWordCount).toBe(false); }); it("accepts descriptive rubric criteria but rejects numeric band-like rubric values", () => { const base = { summary: "Clear feedback", rubric: { taskResponse: { level: "DEVELOPING", feedback: "Address both parts." }, coherenceAndCohesion: { level: "DEVELOPING", feedback: "Use linking." }, lexicalResource: { level: "SECURE", feedback: "Good range." }, grammaticalRangeAndAccuracy: { level: "NEEDS_WORK", feedback: "Check agreement." } }, grammarIssues: [], vocabularyIssues: [], coherenceIssues: [], revisionSuggestions: [], rubricDisclaimer: "Practice feedback only — not an official IELTS band score." }; expect(WritingFeedbackSchema.safeParse(base).success).toBe(true); expect(WritingFeedbackSchema.safeParse({ ...base, rubric: { ...base.rubric, taskResponse: 6.5 } }).success).toBe(false); }); });
