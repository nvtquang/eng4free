import { NextResponse } from "next/server";
import { TutorRequestSchema } from "@english4free/content-schemas";
import { getRequestActor } from "@/modules/auth/request-actor";
import { getAttemptRepository } from "@/modules/attempts/repository";
import { findToeicPart5Question } from "@/modules/exams/toeic-part-5";
import { HttpTutorService } from "@/modules/ai-tutor/tutor-service";
import { allowTutorRequest } from "@/modules/ai-tutor/rate-limit";
export async function POST(request: Request) { const parsed = TutorRequestSchema.safeParse(await request.json().catch(() => null)); if (!parsed.success) return NextResponse.json({ error: "Invalid tutor request" }, { status: 400 }); const { actor } = await getRequestActor(); const attempt = await getAttemptRepository().findById(parsed.data.attemptId); if (!attempt || attempt.status !== "SUBMITTED" || (attempt.userId ? attempt.userId !== actor.userId : attempt.guestId !== actor.guestId)) return NextResponse.json({ error: "Submitted attempt not found" }, { status: 404 }); const question = findToeicPart5Question(parsed.data.questionId); if (!question) return NextResponse.json({ error: "Question not found" }, { status: 404 }); if (!allowTutorRequest(actor.userId ?? actor.guestId)) return NextResponse.json({ error: "Tutor request limit reached" }, { status: 429 }); const optionText = question.content.options.find((option) => option.id === parsed.data.learnerAnswer)?.text ?? null; try { return NextResponse.json(await new HttpTutorService().explain({ prompt: question.content.prompt, learnerAnswer: parsed.data.learnerAnswer, correctOptionId: question.answer.correctOptionId, officialExplanation: question.explanation ?? "Review the official answer.", optionText })); } catch { return NextResponse.json({ error: "Tutor service unavailable" }, { status: 503 }); } }
